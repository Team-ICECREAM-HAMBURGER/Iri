public static class GameControlTypeManager {
    public enum vehicleType {
        TRAIN_PASSENGER,
        TRAIN_FREIGHT,
        TRAIN_DANGEROUS
    }

    public enum TrafficStatus {
        MOVE,
        STOP,
        IDLE
    }
}